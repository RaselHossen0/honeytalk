'use client';

import { Box, Typography } from '@mui/material';

export default function InvitationReviewsPage() {
  return (
    <Box>
      <Typography variant="h5">Invitation Withdrawal Review</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>
        Review queue for invitation withdrawals.
      </Typography>
    </Box>
  );
}
